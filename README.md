Login and Signup form using firebase Authentication, React, material UI and reduxForm.

Run `npm install`, then `npm start`.

Ensure that inside of firebaseConfig.js you add the firebase config object
